# Architecture decision records

ADRs record decisions that constrain implementation or operations. Copy
`0000-template.md`, assign the next four-digit number, and replace every
placeholder. Accepted ADRs are immutable; superseding a decision requires a
new ADR that links to the old one.
